# Session 11 — Optimizer and Learning-Rate Experiments

This report is generated by `optimizer_harness.py` using the deterministic Session 10 TinyGPT.

## 1. Adam by hand

The five-step manual Adam trace agrees with PyTorch to a maximum absolute gap of `4.360e-17`.

## 2. Bias correction

The first-20-step trace is in `bias_correction.png`. Under the 1% relative-update criterion, the difference stops mattering at **not reached by step 20**.

## 3. Warmup and layer ratios

Warmup lasts `6` steps; the update-to-weight ratio is warmup-controlled through step `6` for the logged layers. Ratios are in `layer_update_ratios.png` and `layer_update_ratios.csv`.

## 4. Cosine versus WSD

The tuned schedule selected for the primary comparison is **cosine**. The step-200 and step-300 summaries are below.

| Schedule | LR | Step-200 probe loss | Step-300 probe loss |
|---|---:|---:|---:|
| cosine | 0.001 | 0.574865 ± 0.072311 | 0.174860 ± 0.015296 |
| wsd | 0.0003 | 0.629279 ± 0.012357 | 0.254190 ± 0.014359 |

Keep **cosine** under the predefined step-200 decision rule.

## 5. Width/LR sweep

| Width | Selected LR | Pilot step-200 loss |
|---:|---:|---:|
| 256 | 0.001 | 0.635571 |
| 512 | 0.0003 | 0.230653 |
| 1024 | 0.0001 | 0.232640 |

The extrapolated width-4,096 recommendation is `9.83e-06` with confidence **low**.
The sweep plot marks all three measured minima.

## Reproduction

```bash
python session11/optimizer_harness.py
```
